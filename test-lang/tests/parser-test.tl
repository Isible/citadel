# Test for the parser to see if
# it builds the ast correctly

# <<char>> is a vector of vector of characters (a vector of strings)
fn main(args: <<char>>): void {
    
}

fn test(): string {
    # TODO: add return keyword
    return "Test message :p";
}